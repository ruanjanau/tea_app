import 'package:flutter/material.dart';

class TeaSplash extends StatelessWidget {
  const TeaSplash({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
